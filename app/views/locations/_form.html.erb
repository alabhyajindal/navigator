<%= form_with model: location do |f| %>
  <%= f.label :city %>
  <%= f.text_field :city, autocomplete: "off", autofocus: true %>

  <%= f.label :notes %>
  <%= f.text_area :notes, cols: 30, rows: 10 %>

  <%= f.submit %>
<% end %>