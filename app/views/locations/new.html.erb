<%#= form_with url: locations_path do |f| %>
<%= form_with model: @location do |f| %>
  <%= f.label :city %>
  <%= f.text_field :city, autocomplete: "off" %>

  <%= f.submit %>
<% end %>

<% @locations.each do |l| %>
  <p><%= l.city %></p>
<% end %>